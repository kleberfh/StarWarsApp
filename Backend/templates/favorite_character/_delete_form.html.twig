<form method="post" action="{{ path('favorite_character_delete', {'id': favorite_character.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favorite_character.id) }}">
    <button class="btn">Delete</button>
</form>
